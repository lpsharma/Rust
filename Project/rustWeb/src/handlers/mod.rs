// ============================================================
// handlers/mod.rs — Exports both handler modules
// ============================================================

pub mod pages; // HTML page handlers (returns rendered HTML)
pub mod api;   // REST API handlers  (returns JSON)
